---
import Layout from '~/layouts/PageLayout.astro';
import { SITE } from '~/config.mjs';
import Imprint from '~/components/widgets/Imprint.astro';

const title = `Imprint / Impressum — ${SITE.name}`;

const meta = {
	title: title,
	noindex: true,
};
---

<Layout meta={meta}>
	<Imprint />
</Layout>
